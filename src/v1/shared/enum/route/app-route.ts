export enum AppRoute {
  appointments = 'appointments',
  auth = 'auth',
  core = 'core',
  permissions = 'permissions',
  products = 'products',
  roles = 'roles',
  users = 'users',
  vehicles = 'vehicles',
}
